name: luminix_generator
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.5.2

dependencies:
  build: ^2.4.1
  build_config: ^1.1.1
  change_case: ^2.1.0
  code_builder: ^4.10.0

dev_dependencies:
  build_runner: ^2.4.12
  lints: ^4.0.0
  test: ^1.24.0
